import React from 'react';
import './header.styles.scss';

const Header = () => (
  <header className="header">
    <h1 className="title">Todoist</h1>
    <p className="subtitle">List your todos, tasks, assignments, etc. </p>
  </header>
);

export default Header;
